package br.com.cod3r.cm.campominadofinal;

public class CampoMinadoSwingDefinitivo {
	
	// Programação de interfaces gráficas com usuário para desktop usando swing.
	// Swing é uma biblioteca de componentes visuais multiplataforma.
	
	// Padrão Observer:
	
	/* Em JS é normalmente chamado de "Event Handler" e em Java de "Listener".
	 * JS é fortemente baseado no padrão Observer, a técnica de requisição AJAX
	 *  que faz requisições assíncronas na web, também usa o padrão de projeto Observer.
	 */
	
	// Exemplo na web sem e com o padrão:
	
	// Sem o Observer:
	// Requisição HTTP síncrona, na qual a página fica travada esperando a resposta.
	
	// Com o Observer:
	// Requisição HTTP assíncrona, com AJAX, na qual a página continua livre para fazer outras
	// atividades e quando a resposta chegar, uma notificação será gerada informando o evento.
	
	// Personagens*
	// Observer = Interessado no evento.
	// Subject = Vai detectar o evento e notificar os interessados.
	
	
	// TODO =  Algo para fazer depois no código.
	// FIXME = Error que precisa ser corrigido.
	// Window -> Show View -> Tasks
	
	// Falando agora da implementação do padrão Observer
	// e do uso de Swing como interface gráfica:
	
	/*
	 * Para o projeto "campo-minado-swing" será necessário na classe evento apenas
	 *  uma enumeração que representa o tipo de evento gerado.
	 *  
	 * 
	 */
}
